export default function EnvTest() {
  return (
    <main style={{ padding: 24 }}>
      <div>SUPABASE URL: {process.env.NEXT_PUBLIC_SUPABASE_URL ? "OK" : "MISSING"}</div>
      <div>SUPABASE ANON KEY: {process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY ? "OK" : "MISSING"}</div>
    </main>
  );
}
